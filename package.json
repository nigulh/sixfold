{
  "name": "sixfold",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "prestart": "npm run build && npm run swagger",
    "start": "concurrently \"tsc --watch\" \"nodemon -q dist/server.js\"",
    "test": "jest",
    "swagger": "node src/generateSwagger.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.16.1",
    "rimraf": "^3.0.2",
    "swagger-jsdoc": "^6.1.0",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "jest": "^27.4.7",
    "supertest": "^6.2.2"
  }
}
